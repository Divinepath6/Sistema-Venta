Importante: Configurar la conexion en los archivos del sistema. 
Para colocar la URL ir a la carpeta donde se instale y configurar el archivo con nombre: "Sistema de ventas.exe"
y colocar la URL donde esta la BD 
